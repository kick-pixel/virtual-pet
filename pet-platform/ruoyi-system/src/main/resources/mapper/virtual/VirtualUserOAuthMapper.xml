<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.VirtualUserOAuthMapper">

    <resultMap type="VirtualUserOAuth" id="VirtualUserOAuthResult">
        <id     property="id"               column="id"                />
        <result property="userId"           column="user_id"           />
        <result property="provider"         column="provider"          />
        <result property="providerUserId"   column="provider_user_id"  />
        <result property="providerEmail"    column="provider_email"    />
        <result property="providerName"     column="provider_name"     />
        <result property="providerAvatar"   column="provider_avatar"   />
        <result property="accessToken"      column="access_token"      />
        <result property="refreshToken"     column="refresh_token"     />
        <result property="tokenExpiresAt"   column="token_expires_at"  />
        <result property="rawData"          column="raw_data"          />
        <result property="createTime"       column="create_time"       />
        <result property="updateTime"       column="update_time"       />
    </resultMap>

    <sql id="selectVo">
        select id, user_id, provider, provider_user_id, provider_email, provider_name,
               provider_avatar, access_token, refresh_token, token_expires_at, raw_data,
               create_time, update_time
        from virtual_user_oauth
    </sql>

    <select id="selectById" parameterType="Long" resultMap="VirtualUserOAuthResult">
        <include refid="selectVo"/> where id = #{id}
    </select>

    <select id="selectByProviderAndUserId" resultMap="VirtualUserOAuthResult">
        <include refid="selectVo"/> where provider = #{provider} and provider_user_id = #{providerUserId}
    </select>

    <select id="selectByUserId" parameterType="Long" resultMap="VirtualUserOAuthResult">
        <include refid="selectVo"/> where user_id = #{userId}
    </select>

    <insert id="insertVirtualUserOAuth" parameterType="VirtualUserOAuth" useGeneratedKeys="true" keyProperty="id">
        insert into virtual_user_oauth (user_id, provider, provider_user_id, provider_email, provider_name,
            provider_avatar, access_token, refresh_token, token_expires_at, raw_data, create_time)
        values (#{userId}, #{provider}, #{providerUserId}, #{providerEmail}, #{providerName},
            #{providerAvatar}, #{accessToken}, #{refreshToken}, #{tokenExpiresAt}, #{rawData}, sysdate())
    </insert>

    <update id="updateVirtualUserOAuth" parameterType="VirtualUserOAuth">
        update virtual_user_oauth
        <set>
            <if test="providerEmail != null">provider_email = #{providerEmail},</if>
            <if test="providerName != null">provider_name = #{providerName},</if>
            <if test="providerAvatar != null">provider_avatar = #{providerAvatar},</if>
            <if test="accessToken != null">access_token = #{accessToken},</if>
            <if test="refreshToken != null">refresh_token = #{refreshToken},</if>
            <if test="tokenExpiresAt != null">token_expires_at = #{tokenExpiresAt},</if>
            <if test="rawData != null">raw_data = #{rawData},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        delete from virtual_user_oauth where id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="Long">
        delete from virtual_user_oauth where user_id = #{userId}
    </delete>

</mapper>
